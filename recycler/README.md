# Recycler

Archivos que **no son referenciados** por la aplicación y se movieron aquí para no borrarlos por si acaso.

- **response_didi.json** – Ejemplo/captura de respuesta DiDi (no leído por el código).
- **sitesdidi.json** – Duplicado/alternativo de sedes; la app usa `sedes_didi.json`.
- **DAYLY.JSON** – Posible typo de “daily”; no referenciado.
- **didi/DIDI_SITE_EXAMPLE.JSON** – Solo citado en comentarios, no abierto por código.
- **didi/orders_didi_query copy.json** – Copia; el fallback del código es `orders_didi_query.json` (sin " copy").
- **didi/payload.txt** – Payload de ejemplo/debug, no referenciado.

**HTML (no servidos ni referenciados por la app):**
- **sedes_didi.html**, **didi_inicio.html**, **didi.html** (raíz) – Copias/referencias, no usadas.
- **frontend/didi.html** – Copia guardada de una página DiDi (login/analytics), no parte del build.
- **implementacion didi/seleccionsede.html** – Referencia de la página de selección de sedes; el código no lo abre.

*Se mantiene en su sitio: frontend/index.html (entrada del SPA).*

Si necesitas alguno, muévelo de vuelta a su ubicación original.
