# Copiar a /var/www/restaurant_reports/.env en el servidor
# Backend (uvicorn) - opcional si usas mismo host
# HOST=0.0.0.0
# PORT=8000

# Frontend build ya lleva VITE_API_URL en .env.production
# Backend URL pública (para CORS si el front está en otro dominio)
# BACKEND_URL=https://restaurant.reports.salchimonster.com
